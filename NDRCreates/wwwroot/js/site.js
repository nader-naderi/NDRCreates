// Please see documentation at https://learn.microsoft.com/aspnet/core/client-side/bundling-and-minification
// for details on configuring this project to bundle and minify static web assets.

// Write your JavaScript code.

//window.addEventListener('scroll', function (e) {
//    var div = document.querySelector(".ndrcreates-7981e53bfbf0");
//    var scrollAmount = window.scrollY;
//    var newBackgroundPos = "0% " + (-scrollAmount / 100) + "px";

//    // Check if the background position needs to be updated
//    if (div.style.backgroundPosition !== newBackgroundPos) {
//        div.style.backgroundPosition = newBackgroundPos;
//    }

//    // Add smooth scrolling
//    window.requestAnimationFrame(function (
//    ) {
//        document.body.scrollTop += (scrollAmount / 4);
//    });
//});
